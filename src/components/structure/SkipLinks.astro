<nav id="skip-to" aria-label="Skip links">
  <a href="#main">Skip to main content</a>
  <a href="#footer">Skip to footer</a>
</nav>

<style is:global>
  nav#skip-to {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    clip-path: inset(50%);
    white-space: nowrap;

    &:focus:not(:focus-visible) {
      background-color: transparent;
      outline: none;
    }

    &:is(:focus, :focus-visible) {
      width: auto;
      height: auto;
      margin: unset;
      overflow: visible;
      clip: auto;
      clip-path: none;
      white-space: normal;

      /* Usability and positioning */
      top: 0.5em;
      left: 0.5em;
      z-index: 50;

      /* Visual styles */
      padding: 0.35em 0.45em;
      background-color: var(--);
      outline: 3px solid #0b0c0c;
      outline-offset: 2px;

      & > a {
        font-weight: var(--font-weight-medium);
        text-transform: uppercase;
        text-decoration: none;
        color: var(--sk-color-yellow-digital);
      }
    }
  }
</style>