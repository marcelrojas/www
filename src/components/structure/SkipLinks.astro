<nav id="skip-to-content" aria-label="Skip links">
  <a href="#main" class="skip-to-content">Skip to main content</a>
  <a href="#footer" class="skip-to-content">Skip to footer</a>
</nav>

<style is:global>
  nav#skip-to-content {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    clip-path: inset(50%);
    white-space: nowrap;
    border: 0;

    &:focus:not(:focus-visible) {
      background-color: transparent;
      outline: none
    }

    &:focus, &:focus-visible {
      width: auto;
      height: auto;
      margin: 0;
      overflow: visible;
      clip: auto;
      clip-path: none;
      white-space: normal;

      /* Usability and positioning */
      top: 0.5em;
      left: 0.5em;
      z-index: 50;

      /* Visual styles */
      padding: 0.75em 1em;
      background-color: #fff;
      color: #0b0c0c;
      font-weight: 600;
      text-decoration: none;
      border-radius: 4px;
      outline: 3px solid #0b0c0c;
      outline-offset: 2px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1)
    }
  }
</style>
