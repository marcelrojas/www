---
import Anchor from '@components/Anchor.astro';

const anchors = [
  { href: "/", text: "About" },
  { href: "/services", text: "Services" },
  { href: "/showcase", text: "Showcase" },
  { href: "/resources", text: "Resources" },
];
---

<nav id="haptic-nav" class="navbar" aria-label="Main">
  <ul>
    {anchors.map(link => (
      <li>
        <Anchor variant="gui1" size="large" href={link.href}>{link.text}</Anchor>
      </li>
    ))}
  </ul>
</nav>

<style is:global>
  @layer components {
    nav.navbar {
      position: relative;
      height: min-content;
      width: fit-content;

      & > ul {
        position: relative;
        display: flex;
        flex-flow: row;

        & > li {
          position: relative;
          width: fit-content;

          & > a {
            padding-block: 0.625lh;
            padding-inline: 0.625lh;
            line-height: 1;
          }
        }
      }
    }
  }
</style>
