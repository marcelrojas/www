<div id="logo" aria-hidden="true">
  <span>Marcel</span>
  <span>Rojas</span>
</div>

<style is:global>
  #logo {
    position: relative;
    z-index: 50;
    align-self: self-end;
    max-width: min-content;
    font-family: var(--font-family-serif);
    font-size: var(--font-size-28);
    line-height: var(--font-leading-tightest);
    letter-spacing: var(--font-tracking-common);
    text-align: right;
    text-transform: uppercase;
    text-anchor: middle;
    color: oklch(0.19 0 0 / 0.1);
    animation: logo_view var(--duration-macro-x1) ease-in 0s 1 normal forwards;

    @starting-style {
      opacity: 0;
      visibility: hidden;
    }

  }

  #logo.animate-logo {
    animation: logo_view var(--duration-macro-x1) ease-in 0s 1 normal forwards;
  }

  @keyframes logo_view {

    from {
      opacity: 0;
      visibility: hidden;
    }

    to {
      opacity: 1;
      visibility: visible;
    }

  }
</style>

<script src="@scripts/splashscreen.js"></script>
