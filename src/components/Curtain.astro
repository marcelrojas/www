<div id="curtain"></div>

<style is:global>
  @layer components {
    #curtain {
      display: block;
      position: fixed;
      inset: 0;
      z-index: 10;
      height: 100dvh;
      width: 100dvw;
      background-color: transparent;
      backdrop-filter: blur(0px);
      visibility: hidden;
      user-select: none;
      touch-action: none;
      overscroll-behavior: contain;
      will-change: allow-discrete, backdrop-filter, visibility;
      transition: allow-discrete 0.3s ease-in-out, backdrop-filter 0.3s ease-in-out, visibility 0.3s ease-in-out;
      @starting-style {
        display: block;
        backdrop-filter: blur(0px);
        visibility: hidden
      }
      body:is(.is-menu-open, .is-info-open) & {
        display: none;
        backdrop-filter: blur(24px);
        visibility: visible
      }
    }
  }
</style>